// src/main.c
#include <stdio.h>
#include "submodule/src/submodule.h"  // Include header from submodule

int main() {
// a sample change
    hello_from_submodule();  // Call the function from the submodule
    return 0;
}
